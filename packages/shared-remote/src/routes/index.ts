import { AppRoutes } from '../typings/routes'

export const routes: AppRoutes = {
  home: '/',
  login: '/login',
  register: '/register',
  dashboard: '/dashboard',
  news: '/news',
  learning: '/learning'
}
