import { AppRoutes } from '../typings/rotes'

export const routes: AppRoutes = {
  home: '/',
  login: '/login',
  register: '/register',
  dashboard: '/dashboard',
  news: '/news',
  learning: '/learning'
}
